services:
  api:
    build:
      context: ./api
    container_name: basic_agent_api
    ports:
      - "8001:8000"
    env_file:
      - .env
    restart: unless-stopped

  ui:
    build:
      context: ./ui
    container_name: basic_agent_ui
    ports:
      - "8501:8501"
    depends_on:
      - api
    restart: unless-stopped
