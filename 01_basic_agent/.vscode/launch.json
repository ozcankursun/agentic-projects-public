{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug API (Attach)",
      "type": "debugpy",
      "request": "attach",
      "connect": {
        "host": "localhost",
        "port": 5678
      },
      "pathMappings": [
        {
          "localRoot": "${workspaceFolder}/01_basic_agent/api",
          "remoteRoot": "${workspaceFolder}/01_basic_agent/api"
        }
      ]
    }
  ]
}
